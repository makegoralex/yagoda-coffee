# yagoda-coffee
Сайт кофейни «Ягода».

## Запуск

```bash
npm install
npm run dev
```

## Деплой

Workflow собирает проект командой `npm run build`. Убедитесь, что веб-сервер
отдаёт статические файлы из папки `dist`.
